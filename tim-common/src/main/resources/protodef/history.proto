syntax = "proto3";
package com.tim.common.protos;
import "common.proto";

option java_outer_classname = "History";

// 历史消息请求  消息ID使用客户端生成
message HisMessagesReq {
  uint64 id = 1; // 消息ID
  string converId = 2; // 会话id
}

// 历史消息
message HisMessagesAck {
  uint64 id = 1; // 消息ID
  string converId = 2; // 会话id
  repeated MessageContent messageList = 3;
}
